# Example environment configuration for Walrus S3 Gateway Test
# Copy this file to .env and modify as needed

# Gateway Configuration
# The GATEWAY_URL is automatically composed from GATEWAY_PROTOCOL, GATEWAY_HOST, and GATEWAY_PORT
# You can override GATEWAY_URL directly if needed, but it's recommended to use the components
GATEWAY_PROTOCOL=http
GATEWAY_HOST=127.0.0.1
GATEWAY_PORT=8080
# GATEWAY_URL=http://127.0.0.1:8080  # Optional: override computed URL

# S3 Credentials
ACCESS_KEY=walrus-access-key
SECRET_KEY=walrus-secret-key
AWS_REGION=us-east-1

# Test Configuration
TEST_BUCKET=my-test-bucket
TEST_OBJECT=my-test-file.txt
TEST_CONTENT="Custom test content for my test"

# Sui Network Configuration
SUI_NETWORK=testnet
SUI_RPC_URL=https://fullnode.testnet.sui.io:443

# Timeout Configuration (in seconds)
REQUEST_TIMEOUT=30
FAUCET_TIMEOUT=30
FAUCET_WAIT_TIME=10

# Cleanup Configuration
AUTO_CLEANUP=true

# Custom Wallet Directory (optional)
# WALLET_DIR=/tmp/my-wallet

# Examples for different environments:
# 
# For local development on different port:
# GATEWAY_HOST=localhost
# GATEWAY_PORT=9000
# 
# For HTTPS gateway:
# GATEWAY_PROTOCOL=https
# GATEWAY_HOST=my-gateway.com
# GATEWAY_PORT=443
#
# For mainnet testing:
# SUI_NETWORK=mainnet
# SUI_RPC_URL=https://fullnode.mainnet.sui.io:443
#
# For devnet testing:
# SUI_NETWORK=devnet
# SUI_RPC_URL=https://fullnode.devnet.sui.io:443
