[package]
name = "WAL_exchange"
license = "Apache-2.0"
authors = ["Mysten Labs <build@mystenlabs.com>"]
edition = "2024.beta"

[dependencies]
WAL = { local = "../wal" }

[addresses]
wal_exchange = "0x0"
