[package]
name = "Subsidies"
license = "Apache-2.0"
authors = ["Mysten Labs <build@mystenlabs.com>"]
edition = "2024.beta"

[dependencies]
WAL = { local = "../wal" }
Walrus = { local = "../walrus" }

[addresses]
subsidies = "0x0"
