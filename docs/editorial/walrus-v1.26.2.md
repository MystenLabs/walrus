---
title: Walrus v1.26.2
description: The Testnet build introducing multi-URL Sui RPC configuration and the breaking change where metrics and REST API endpoints bind to the configured...
keywords: ["walrus", "release notes", "changelog", "testnet"]
---

**Testnet** | May 28, 2025

The Testnet build introducing multi-URL Sui RPC configuration and the breaking change where metrics and REST API endpoints bind to the configured address rather than always to `0.0.0.0`.
