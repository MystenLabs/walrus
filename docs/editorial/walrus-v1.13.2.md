---
title: Walrus v1.13.2
description: Fixes the blob-certified metric so it updates only for event blobs and makes the publisher retry when a refresh fails.
keywords: ["walrus", "release notes", "changelog", "testnet", "publisher", "blob"]
---

**Testnet** | February 13, 2025

Fixes the blob-certified metric so it updates only for event blobs and makes the publisher retry
when a refresh fails.
