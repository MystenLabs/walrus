---
title: Walrus v1.33.3
description: Adds the aggregator --max-blob-size option and marks the switch to deletable-by-default blobs; pass permanent=true (or...
keywords: ["walrus", "release notes", "changelog", "mainnet", "aggregator", "blob"]
---

**Mainnet** | September 29, 2025

Adds the aggregator `--max-blob-size` option and marks the switch to deletable-by-default blobs;
pass `permanent=true` (or `--permanent`) to retain permanent storage.
